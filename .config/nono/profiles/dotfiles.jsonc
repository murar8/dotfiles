{
  "$schema": "../profile.schema.json",
  "extends": ["default", "commit"],
  "meta": {
    "name": "dotfiles",
    "version": "0.1.0",
    "description": "The ~/.dotfiles repo, whose worktree is $HOME, so tracked subtrees need read here.",
    "author": "murar8"
  },
  "filesystem": {
    "allow": ["$HOME/.dotfiles"]
  }
}
