{
  "$schema": "../profile.schema.json",
  "extends": ["default", "commit"],
  "meta": {
    "name": "dotfiles",
    "version": "0.1.0",
    "description": "Work on the ~/.dotfiles repo, whose worktree is $HOME, so every tracked subtree that needs committing must be granted read here.",
    "author": "murar8"
  },
  "filesystem": {
    "allow": ["$HOME/.dotfiles"],
    "read": ["$HOME/.config/nono"]
  }
}
