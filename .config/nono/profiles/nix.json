{
  "$schema": "../profile.schema.json",
  "meta": {
    "name": "nix",
    "version": "0.2.0",
    "description": "Nix toolchain overlay. Composed onto a base profile via --extends; grants the daemon socket (every build and store query goes through it), the client's own caches/state, the treefmt cache that nix fmt writes, and the substituter hosts this machine configures. /nix/store stays read-only — the daemon owns writes.",
    "author": "murar8"
  },
  "groups": {
    "include": ["nix_runtime"]
  },
  "filesystem": {
    "allow": [
      "$HOME/.cache/nix",
      "$HOME/.cache/treefmt",
      "$HOME/.local/state/nix",
      "$HOME/.local/share/nix"
    ],
    "unix_socket": ["/nix/var/nix/daemon-socket/socket"]
  },
  "network": {
    "allow_domain": [
      "cache.nixos.org",
      "channels.nixos.org",
      "nixos.org",
      "nix-community.cachix.org",
      "cache.numtide.com",
      "microvm.cachix.org",
      "fenix.cachix.org"
    ]
  }
}
