{
  "$schema": "../profile.schema.json",
  "extends": "default",
  "meta": {
    "name": "commit",
    "version": "0.3.0",
    "description": "Signs commits and tags via the ssh-agent socket and lets pre-commit reuse its hook envs, without granting the private key.",
    "author": "murar8"
  },
  "filesystem": {
    "allow": ["$HOME/.cache/pre-commit"],
    "unix_socket": ["$XDG_RUNTIME_DIR/gcr/ssh"]
  }
}
