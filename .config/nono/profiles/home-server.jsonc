{
  "$schema": "../profile.schema.json",
  "extends": ["nix", "commit"],
  "meta": {
    "name": "home-server",
    "version": "0.1.0",
    "description": "The home-server tree, with its own GitHub token brokered supervisor-side.",
    "author": "murar8",
  },
  "filesystem": {
    "allow": ["$HOME/Documents/Personal/home-server", "$HOME/.cache/treefmt"],
  },
  "network": {
    "allow_domain": [
      "static.perimeter81.com",
      "dl.google.com",
      "downloads.mongodb.com",
      "downloads.slack-edge.com",
    ],
    "credentials": ["github_home_server"],
    "custom_credentials": {
      "github_home_server": {
        "upstream": "https://api.github.com",
        "credential_key": "github_home_server",
        "env_var": "GH_TOKEN",
        "inject_header": "Authorization",
        "credential_format": "Bearer {}",
      },
    },
  },
}
