{
  "$schema": "../profile.schema.json",
  "extends": "nolabs-ai/pi",
  "meta": {
    "name": "pi-local",
    "version": "1.0.0",
    "description": "Local override of the registry pi profile. Adds OpenRouter credential injection: the API key stays supervisor-side; the sandbox only ever sees a phantom. Mirrors opencode-local, and reuses the same 'openrouter' keystore account.",
    "author": "murar8"
  },
  "filesystem": {
    "allow": ["/tmp/jiti"]
  },
  "network": {
    "allow_domain": ["pi.dev"],
    "credentials": ["openrouter"],
    "custom_credentials": {
      "openrouter": {
        "upstream": "https://openrouter.ai/api/v1",
        "credential_key": "openrouter",
        "env_var": "OPENROUTER_API_KEY",
        "inject_header": "Authorization",
        "credential_format": "Bearer {}"
      }
    }
  }
}
